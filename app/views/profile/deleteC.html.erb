<% @page_title = "Delete #{@content.title}"%>

<%= link_to("<<Beack to Happy Place", {:action => 'show',:user_id => @user.id}, :class => 'back-link')%>
	
<div class="user delete">
	<h2>Delete <%=@content.title%></h2>
	
	<%= form_for(:content, :url => {:action => 'explodePineapple', :id => @content.id, :user_id => @content.user_id}) do |f| %>

	<p>Are you sure you want to permanently delete this content?</p>

	<p class="reference-name"><%= @content.title %></p>

	<div class="form-buttons"> 
		<%= submit_tag("Delete Content") %>
	</div>
	
	<%end%>
</div>
